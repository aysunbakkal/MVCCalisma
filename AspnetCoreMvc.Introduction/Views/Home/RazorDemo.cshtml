@model AspnetCoreMvc.Introduction.Models.EmployeeListViewModel

@{
    Layout = null;
    var value = 10;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>RazorDemo</title>
</head>
<body>
    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>City Id</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Employees)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.FirstName</td>
                    <td>@item.LastName</td>
                    <td>@item.CityId</td>
                </tr>
            }
        </tbody>
    </table>
    Mail me: aysun.bakkal16@gmail.com
    me: @@home razor demo algılanmaması için çift @@ kullanılır..
    <p>@DateTime.Now</p>
    @{ var date = DateTime.Now - TimeSpan.FromDays(3);}
    <p>3 days ago was @date </p>
    @{
        var number = 10;
    }
    <p>@number</p>

    @if (number < 10)
    {
        <p>Your number is less than 10</p>
    }
    else if (number == 10)
    {
        <p>Your number is 10</p>
    }
    else
    {
        <p>Your number is greater than 10</p>
    }
    FOR
    @for (int i = 0; i < Model.Employees.Count; i++)
    {
        <br /> <b>@Model.Employees[i].FirstName</b>
    }
    @{ var counter = 0;}
    WHILE
    @while (counter < Model.Employees.Count)
    {
        <br /> <b>@Model.Employees[counter].FirstName</b>
        counter++;
    }
    DO WHILE
    @{ var counter2 = 0;}
    @do
    {
        <br /> <b>@Model.Employees[counter2].FirstName</b>
        counter2++;
    } while (counter2 < Model.Employees.Count);

    HATA AYIKLAMA

    @try
    {

    }
    catch (ArgumentNullException argumentNullException)
    {

        throw;
    }
    catch (Exception Exception)
    {

        throw;
    }
    finally
    {

    }

    USING: Bir nesnenin işlemi bittiğinde otomatik bellekten silinir.
    Butona bastığımızda bu formun ilgili aksiyona geçmesi. 
    

    @using (Html.BeginForm()) 
    {
        <input type="text" id="" name="" />
        <button type="submit"></button>
    }
    
    Model Binding : Bir aksiyon çalıştığında ona parametre gönderir.
</body>
</html>
